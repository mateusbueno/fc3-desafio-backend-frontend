export interface Route {
    title: string;
    startPosition: {
        latitude: string,
        longitude: string
    };
    endPosition: {
        latitude: string,
        longitude: string
    };
}
